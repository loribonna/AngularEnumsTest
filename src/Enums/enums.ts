export namespace Enums {
    export enum A {
        a,
        b,
        c
    }

    export enum B {
        a,
        b,
        c
    }

    export const D = A.a;

}


export enum C {
    a,
    b,
    c
}
