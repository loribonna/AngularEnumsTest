# AngularEnumsTest

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 6.0.1.

This project serves as example to test Enums exportation

## Run project

Run `npm start` to run with '--prod' flag.

## Check exported value

Just open console
